import { lineThicknessPx } from "src/css.ts";

export const SIZE_PX = 6;

export const patternWidthPx = SIZE_PX * 4;
export const patternHeightPx = SIZE_PX + lineThicknessPx;

export const startY = lineThicknessPx / 2;
export const endY = SIZE_PX + lineThicknessPx / 2;

export const dividerHeightPx = patternHeightPx;
