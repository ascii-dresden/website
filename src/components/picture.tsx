import type { Component } from "solid-js";

/**
 * This function is computationally expensive.
 * While possible to use in SSR (per-request), you probably want to always prerender this.
 */
export const Picture: Component = function (props) {
	return <></>;
};
