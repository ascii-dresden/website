---
import '@fontsource/concert-one';
import '@fontsource-variable/nunito';
import '@fontsource-variable/jetbrains-mono';

import 'src/styles/global/mod.css.ts';

import { main } from './base.css.ts';

import Header from './base/header.astro';
import Footer from './base/footer.astro';
---

<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>ascii</title>
	</head>

	<body>
		<Header />
		<main class={main}>
			<slot />
		</main>
		<Footer />
	</body>
</html>

<script>
	import { loadTheme, setTheme } from 'src/theme.ts';
	setTheme(loadTheme());
</script>
